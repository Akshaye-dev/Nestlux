import { StyleSheet, Text, View } from "react-native";

const InputText = () => {
  return (
    <View >
      <Text>InputText</Text>
    </View>
  );
};

export default InputText;

const styles = StyleSheet.create({});
